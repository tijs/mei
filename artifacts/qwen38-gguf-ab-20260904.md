# Qwen3.8-27B GGUF/llama.cpp same-suite reference A/B vs Mei MLX 4-bit (todo 0b87b76a#11 leg) — 2026-09-04

Unit: todo 11 (common matrix per loadable model vs finalized GGUF/llama.cpp
references) — Qwen3.8-27B reference leg, the last model missing a full
same-suite GGUF row (short-decode ceiling 2026-09-02 was on record; coding,
tool, and 30k long-context rows were not). Clean window: zero foreign
inference processes and no listening benchmark ports before/during the run;
Mei-owned port 8078, runtime logs under
`~/.local/share/local-model-bench/mei-runtime-q38-ref/` (Mei-owned disposable
dirs; local-model-bench READ-ONLY). GGUF blob sha256 re-verified fresh this
tick: `2de73110cb254cbf09b54b717578dadff12ef1194e7271527e68202f39ba4bfd` ==
pinned HF LFS blob.

## Reference (content-pinned)

| model | GGUF | sha256 (== LFS blob) | size | arch | blocks | MTP |
|---|---|---|---|---|---|---|
| Qwen3.8-27B | unsloth/Qwen3.8-27B-GGUF UD-Q5_K_M rev 4ca720788d1e | `2de73110…4bfd` | 19,771,509,664 B | qwen35 | 65 (64+1 MTP head, nextn=1) | present, run WITHOUT `--spec-type` (single-token decode, comparable to Mei no-MTP baseline) |

Engine: `/opt/homebrew/bin/llama-server` build 10470 (commit 34af94cd9),
`--ctx-size 65536`, temp 0, top-p 0.95, top-k 20, `--parallel 1 --no-webui
--metrics`, port 8078 — identical engine/config class as the 2026-09-02
ceiling run (port 8075) and the Ornith/Gemma/Heretic reference legs.

Mei MLX side (on-record, same proven safe config: generic profile, cap 65536,
prefill 64, kv-bits none, cache-reuse true, disposable disk KV, port 8024,
temp 0): `mlx-community/Qwen3.8-27B-4bit` @ `3e6447f0`, binary
`8051d806ce875ab8` (raw-endpoint [1,T] fix `ee7368d` build) — common matrix
COMPLETE chat+raw (2026-09-03, artifacts in qwen38-4bit-common-matrix-20260903.md).

## Measured — GGUF/llama.cpp side (2026-09-03 ~21:02–21:14 UTC)

### Same-suite rows (artifacts/gguf-ref-qwen38-ud-q5-20260904.json)

| row | result | measured |
|---|---|---|
| coding 4/4 (swift_fibonacci, python_json_sum, sql_users_query, shell_rename) | **PASS** | decode 9.330 / 9.318 / 9.395 / 9.399 t/s |
| native tool call `add_pair` "What is the sum of 15 and 27?" | **PASS** | arguments `{"a":15,"b":27}` (JSON ints, GGUF-native), RSS 25.03 GB |
| long-context 30k FRESH fill (raw, self-calibrated 29,703 tok) | **PASS** | 358.1 s → **82.70 pps**, RSS 24.31 GB |
| long-context 30k REUSE (llama.cpp slot cache) | **PASS** | 29,699/29,703 cached, request 0.31 s |
| identity limerick | recorded | refusal_cues [], decode 9.29 t/s |

### 30k degenerate-continuation note (same family behavior as Heretic, 2026-09-03)

The pure `"hello "*N` continuation after the 29.7k fill stopped after 1 token
(empty completion, EOS at step 1) — deterministic, reproduced 3× (probe row +
2 manual repeats, max_tokens 64 and 128). NOT a server/memory defect: same
session generated 200 tokens (identity) and 75 (tool); with a continuation
suffix (`"Now continue: 1, 2, 3,"`) the same 29.7k prompt decodes normally
2× at 8.301/8.298 t/s (mean 8.30, sd 0.0015, cached 29,711/29,715, RSS 24.24
GB) — artifacts/qwen38-30k-suffix-retest-20260904.json. Identical pattern was
recorded for the Heretic Q5_K_M GGUF leg (gguf-ref-heretic-30k-decode-retest-
20260903.json, 7.65 t/s). Mei MLX 4-bit does NOT degenerate on the pure fill:
generation 32 tokens at 11.61 t/s (rawfix 30k fresh row).

## A/B vs Mei MLX 4-bit (on-record common-matrix rows)

| row | GGUF/llama.cpp UD-Q5_K_M | Mei MLX 4-bit @3e6447f0 | delta |
|---|---|---|---|
| short decode (3x mean) | 9.286 t/s (sd 0.001; ceiling 2026-09-02, port 8075; coding rows this tick 9.32–9.40 confirm the band) | 15.656 t/s (sd 0.060; probe-load r1–r3) | MLX **+68.6%** |
| coding 4/4 | PASS @ 9.32–9.40 t/s | PASS @ ~15.6 t/s (probe-coding-…T225610Z) | MLX **+66%** |
| tool call (15+27) | PASS `{"a":15,"b":27}` ints | PASS — acceptance 12/12 incl. tool non+stream (a=15,b=27) | parity |
| 30k fresh prefill | 82.70 pps (358 s) | 55.8 pps (541 s; rawfix row) | GGUF **+48%** |
| 30k loaded decode | 8.30 t/s (suffix retest 2x; pure fill degenerates to EOS) | 11.61 t/s fresh / 11.76 t/s reuse (no degeneracy) | MLX **+40–54%** |
| 30k reuse | slot cache 29,699/29,703, 0.31 s request | disk-KV 30,000/30,001, restore 4.7 s | different mechanisms (llama.cpp slot vs Mei disk tier) — recorded, never claimed equal |
| peak memory @30k/fill | RSS 24.31 GB (24.31–25.03 GB rows) | peak 31.70 GB raw fill, active 27.25 GB | GGUF lower peak; both < 32 GB |
| decode at 65k cap | n/a (not run) | 31.70 GB peak, PASS | — |

## Interpretation

- Direction matches every prior model A/B: Mei MLX beats llama.cpp on decode
  (+68.6% short on 4-bit; +41% on 5-bit 2026-09-02), llama.cpp wins chunked
  prefill for this dense hybrid (+48%). Both engines far below 30 t/s at 30k
  (8.3–11.8 t/s) — consistent with the documented exhaustive hardware ceiling
  for the Qwen3.8-27B family (64-layer dense hybrid, per-token GDN recurrence,
  M1 Max ~350–400 GB/s), already ACCEPTED in the plan record (2026-09-02).
  This leg adds the 30k long-context reference dimension to that record; no
  target re-opened.
- GGUF-side caveats recorded, not swept: UD-quant degenerate pure-fill
  continuation (EOS), slot-cache vs disk-KV semantics differ, 30k decode rows
  are 2 repeats (sd 0.0015), short decode row cites the verified 2026-09-02
  ceiling (same blob/binary/config).

## Todo status

With this leg, EVERY loadable model has its common correctness matrix AND a
same-suite GGUF/llama.cpp reference row: Ornith-35B (55c865a), Gemma 4
(e6f892d + bb811ed), Heretic (8ec69d3 + retest), Qwen3.8-27B (this leg +
on-record ceiling/common matrix). Todo `0b87b76a#11` measurable legs are
COMPLETE. Residuals recorded (not worker-measurable): (a) vmlx fork 318a4e68
(Gemma growing-reuse fix) un-pushed — remote pin stays 91fed8be, local edit
active on Sulaco build; (b) Qwen 4-bit-vs-5-bit default decision recorded in
the plan's speed-gate acceptance (5-bit stays the documented parity artifact);
(c) Ornith-9B proxy (not a finalist) matrix legs predate the 2026-09-03
hardening — refresh queued under #12 only if the proxy becomes active again.

## Reproducibility

```bash
GGUF=$HOME/.cache/huggingface/hub/models--unsloth--Qwen3.8-27B-GGUF/snapshots/\
4ca720788d1e01f1bff70c033e0d0028fd02e502/Qwen3.8-27B-UD-Q5_K_M.gguf
/opt/homebrew/bin/llama-server --model "$GGUF" --host 127.0.0.1 --port 8078 \
  --ctx-size 65536 --temp 0 --top-p 0.95 --top-k 20 \
  --alias qwen38-27B-UD-Q5_K_M --parallel 1 --no-webui --metrics
cd /Users/tijs/projects/mei && python3 tools/gguf_ref_probe.py \
  --base-url http://127.0.0.1:8078/v1 --model qwen38-27B-UD-Q5_K_M \
  --pid <llama-server pid> --output artifacts/gguf-ref-qwen38-ud-q5-20260904.json \
  --timeout 2400
# suffix retest: raw /v1/completions, prompt = ('hello '*29702 + 'Now continue: 1, 2, 3,'),
# temp 0, max_tokens 64 -> 8.30/8.30 t/s (rows in qwen38-30k-suffix-retest-20260904.json)
```

Server killed at leg end; port 8078 free for the next worker.